package com.sureshragam.smarthomecamera.config

object AppConfig {

    // Spring Boot Backend
    const val DEVICE_CODE = "ANDROID_CAMERA-LIVING_ROOM-001"

    const val BASE_URL = "https://api.smarthome.sureshragam.in/"
    const val APP_VERSION = "1.0.0"
    // Tablet local stream port
    const val STREAM_PORT = 8081

    // Heartbeat interval
    const val HEARTBEAT_INTERVAL = 30000L

    // Device type
    const val DEVICE_TYPE = "ANDROID_CAMERA"
}